

export default function Header() {
    return (
        <header className="flex flex-row justify-between items-center">
            <button>Simple Task</button>
            <nav></nav>
            <button>Se connecter</button>
        </header>
    );
}